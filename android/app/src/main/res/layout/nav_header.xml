<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/nav_header_bg"
    android:padding="20dp"
    android:gravity="center_horizontal">

    <!-- App Icon with subtle shadow -->
    <ImageView
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:src="@drawable/ic_air_mouse_pro_ultra"
        android:layout_gravity="center"
        android:elevation="4dp"
        android:contentDescription="@string/app_name" />

    <!-- App Name -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textColor="@android:color/white"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_gravity="center"
        android:layout_marginTop="12dp"
        android:shadowColor="#22000000"
        android:shadowDx="1"
        android:shadowDy="1"
        android:shadowRadius="2" />

    <!-- Version / Build info -->
    <TextView
        android:id="@+id/nav_version_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Version 3.0.0"
        android:textColor="#CCFFFFFF"
        android:textSize="12sp"
        android:layout_gravity="center"
        android:layout_marginTop="4dp" />

    <!-- Divider -->
    <View
        android:layout_width="60dp"
        android:layout_height="2dp"
        android:background="@color/accent_blue"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="8dp" />

    <!-- Short description / tagline -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="AI‑powered remote control"
        android:textColor="#B3FFFFFF"
        android:textSize="11sp"
        android:layout_gravity="center"
        android:gravity="center"
        android:alpha="0.8" />

    <!-- Optional: Connection status (can be updated dynamically in code) -->
    <TextView
        android:id="@+id/nav_connection_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="● Disconnected"
        android:textColor="#FFA0A0"
        android:textSize="10sp"
        android:layout_gravity="center"
        android:layout_marginTop="8dp"
        android:visibility="gone" />

</LinearLayout>