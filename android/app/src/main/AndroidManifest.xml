<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.airmouse">

    <!-- ==================== PERMISSIONS ==================== -->

    <!-- Bluetooth -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"
        tools:targetApi="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        tools:targetApi="31" />

    <!-- Network & Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- Camera (QR code scanning) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Haptic Feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- System Overlay (debug overlay) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Location (required for Bluetooth scanning on Android < 12) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Foreground Services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- Sensors -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />

    <!-- Storage (Photos/Videos Access) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- USB -->
    <uses-permission android:name="android.permission.USB_PERMISSION" />

    <!-- Audio -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Wake Lock -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Boot Completed -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- ==================== FEATURE DECLARATIONS ==================== -->

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.sensor.gyroscope"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.sensor.accelerometer"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.usb.accessory"
        android:required="false" />

    <application
        android:name=".AirMouseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AirMouse"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:largeHeap="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:appCategory="game"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="34">

        <!-- ==================== ACTIVITIES ==================== -->

        <!-- Main Activity - Launcher -->
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden|keyboard|locale|uiMode"
            android:theme="@style/Theme.AirMouse.Splash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep link for pairing -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="airmouse"
                    android:host="connect" />
                <data
                    android:scheme="airmouse"
                    android:host="pair" />
            </intent-filter>
        </activity>

        <!-- Calibration Activity -->
        <activity
            android:name=".presentation.calibration.CalibrationActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AirMouse"
            android:configChanges="orientation|screenSize" />

        <!-- Settings Activity -->
        <activity
            android:name=".presentation.ui.settings.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".presentation.MainActivity"
            android:theme="@style/Theme.AirMouse"
            android:configChanges="orientation|screenSize" />

        <!-- Onboarding Activity -->
        <activity
            android:name=".presentation.ui.onboarding.OnboardingActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AirMouse.Splash"
            android:configChanges="orientation|screenSize" />

        <!-- Gesture Studio Activity -->
        <activity
            android:name=".presentation.ui.gesture.GestureStudioActivity"
            android:exported="false"
            android:parentActivityName=".presentation.MainActivity"
            android:theme="@style/Theme.AirMouse"
            android:configChanges="orientation|screenSize" />

        <!-- Proximity Activity -->
        <activity
            android:name=".presentation.ui.proximity.ProximityActivity"
            android:exported="false"
            android:parentActivityName=".presentation.MainActivity"
            android:theme="@style/Theme.AirMouse"
            android:configChanges="orientation|screenSize" />

        <!-- Statistics Activity -->
        <activity
            android:name=".presentation.ui.statistics.StatisticsActivity"
            android:exported="false"
            android:parentActivityName=".presentation.MainActivity"
            android:theme="@style/Theme.AirMouse"
            android:configChanges="orientation|screenSize" />

        <!-- Profiles Activity -->
        <activity
            android:name=".presentation.ui.profiles.ProfilesActivity"
            android:exported="false"
            android:parentActivityName=".presentation.MainActivity"
            android:theme="@style/Theme.AirMouse"
            android:configChanges="orientation|screenSize" />

        <!-- Voice Commands Activity -->
        <activity
            android:name=".presentation.ui.voice.VoiceCommandsActivity"
            android:exported="false"
            android:parentActivityName=".presentation.MainActivity"
            android:theme="@style/Theme.AirMouse"
            android:configChanges="orientation|screenSize" />

        <!-- Edge Gestures Activity -->
        <activity
            android:name=".presentation.ui.edge.EdgeGesturesActivity"
            android:exported="false"
            android:parentActivityName=".presentation.MainActivity"
            android:theme="@style/Theme.AirMouse"
            android:configChanges="orientation|screenSize" />

        <!-- Themes Activity -->
        <activity
            android:name=".presentation.ui.themes.ThemesActivity"
            android:exported="false"
            android:parentActivityName=".presentation.MainActivity"
            android:theme="@style/Theme.AirMouse"
            android:configChanges="orientation|screenSize" />

        <!-- Logs Activity -->
        <activity
            android:name=".presentation.ui.logs.ServerLogsActivity"
            android:exported="false"
            android:parentActivityName=".presentation.MainActivity"
            android:theme="@style/Theme.AirMouse"
            android:configChanges="orientation|screenSize" />

        <!-- Network Discovery Activity -->
        <activity
            android:name=".presentation.ui.network.NetworkDiscoveryActivity"
            android:exported="false"
            android:parentActivityName=".presentation.MainActivity"
            android:theme="@style/Theme.AirMouse"
            android:configChanges="orientation|screenSize" />

        <!-- Touchpad Activity -->
        <activity
            android:name=".presentation.ui.touchpad.TouchpadActivity"
            android:exported="false"
            android:parentActivityName=".presentation.MainActivity"
            android:theme="@style/Theme.AirMouse"
            android:configChanges="orientation|screenSize" />

        <!-- About Activity -->
        <activity
            android:name=".presentation.ui.about.AboutActivity"
            android:exported="false"
            android:parentActivityName=".presentation.MainActivity"
            android:theme="@style/Theme.AirMouse"
            android:configChanges="orientation|screenSize" />

        <!-- Update Activity -->
        <activity
            android:name=".presentation.ui.update.UpdateActivity"
            android:exported="false"
            android:parentActivityName=".presentation.MainActivity"
            android:theme="@style/Theme.AirMouse"
            android:configChanges="orientation|screenSize" />

        <!-- ==================== SERVICES ==================== -->

        <!-- Sensor Service -->
        <service
            android:name=".service.SensorService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:process=":sensor" />

        <!-- Gesture Inference Service -->
        <service
            android:name=".service.GestureInferenceService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:process=":gesture" />

        <!-- Gesture Recorder Service -->
        <service
            android:name=".service.GestureRecorderService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Bluetooth HID Service -->
        <service
            android:name=".service.BluetoothHidService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Proximity Service -->
        <service
            android:name=".service.ProximityService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:process=":proximity" />

        <!-- Proximity Aware Service -->
        <service
            android:name=".service.ProximityAwareService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- USB HID Service -->
        <service
            android:name=".service.UsbHidService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- USB Serial Service -->
        <service
            android:name=".service.UsbSerialService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Voice Command Service -->
        <service
            android:name=".service.VoiceCommandService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone"
            android:process=":voice" />

        <!-- Orientation Monitor Service -->
        <service
            android:name=".service.OrientationMonitorService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Edge Gesture Service -->
        <service
            android:name=".service.EdgeGestureService"
            android:enabled="true"
            android:exported="false" />

        <!-- Debug Overlay Service -->
        <service
            android:name=".service.DebugOverlayService"
            android:enabled="true"
            android:exported="false" />

        <!-- Foreground Service Manager -->
        <service
            android:name=".service.ForegroundServiceManager"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- ==================== RECEIVERS ==================== -->

        <!-- Boot Completed Receiver -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- USB Device Attached Receiver -->
        <receiver
            android:name=".receivers.UsbReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </receiver>

        <!-- Bluetooth Receiver -->
        <receiver
            android:name=".receivers.BluetoothReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.bluetooth.adapter.action.STATE_CHANGED" />
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED" />
                <action android:name="android.bluetooth.device.action.ACL_DISCONNECTED" />
            </intent-filter>
        </receiver>

        <!-- Network Receiver -->
        <receiver
            android:name=".receivers.NetworkReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <!-- Notification Receiver -->
        <receiver
            android:name=".receivers.NotificationReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- ==================== PROVIDERS ==================== -->

        <!-- File Provider for sharing files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- ==================== METADATA ==================== -->

        <!-- Google Play Services -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- Firebase -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />

    </application>

</manifest>